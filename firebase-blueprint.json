{
  "entities": {
    "quote": {
      "title": "Quote Request",
      "description": "Information provided by a user requesting an insurance quote.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "Full name of the requester" },
        "email": { "type": "string", "format": "email", "description": "Email address of the requester" },
        "phone": { "type": "string", "description": "Phone number of the requester" },
        "address": { "type": "string", "description": "Mailing address of the requester" },
        "coverageTypes": { "type": "array", "items": { "type": "string" }, "description": "Types of insurance coverage requested" },
        "fileName": { "type": "string", "description": "Name of the uploaded declarations page file (metadata only here)" },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp when the quote was submitted" },
        "status": { "type": "string", "enum": ["new", "reviewed", "archived"], "description": "Current status of the quote request" }
      },
      "required": ["name", "email", "phone", "address", "createdAt", "status"]
    }
  },
  "firestore": {
    "/quotes/{quoteId}": {
      "schema": "quote",
      "description": "Collection of quote requests submitted via the lead form."
    }
  }
}
